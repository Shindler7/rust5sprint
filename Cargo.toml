[package]
name = "broken-app"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.10"

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "baseline"
harness = false

[profile.release]
debug = true

[features]
benchmark = []
