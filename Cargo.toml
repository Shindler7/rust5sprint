[package]
name = "broken-app"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
rand = "0.10"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "baseline"
harness = false

[profile.release]
debug = true

[features]
benchmark = []
